# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove /vendor/ if you want to skip it)
/vendor/ 

# Go workspace file
go.work
go.work.sum

# Env files (NEVER commit secrets)
.env
.env.local
.env.*.local

# Docker / Air temporary files
tmp/
bin/
main

# IDE Specific
.idea/
.vscode/
*.swp
*.swo
.qodo

# OS Specific
.DS_Store
Thumbs.db